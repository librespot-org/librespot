// Extracted from: Spotify 1.1.33.569 (Windows)

syntax = "proto3";

package spotify.profile.proto;

import "identity.proto";

option optimize_for = CODE_SIZE;

message CachedProfile {
    identity.proto.DecorationData profile = 1;
    int64 expires_at = 2;
    bool pinned = 3;
}

message ProfileCacheFile {
    repeated CachedProfile cached_profiles = 1;
}
