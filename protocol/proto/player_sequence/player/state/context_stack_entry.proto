// Extracted from: Spotify 1.2.96.518 (Windows)

syntax = "proto3";

package spotify.player.sequence.proto;

option optimize_for = CODE_SIZE;

message ContextStackEntry {
    oneof _selected_child_uid {
        string selected_child_uid = 1;
    }
}
