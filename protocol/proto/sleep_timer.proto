// Extracted from: Spotify 1.2.96.518 (Windows)

syntax = "proto2";

package spotify.player.proto.transfer;

option optimize_for = CODE_SIZE;

message SleepTimer {
    oneof timer_type {
        None none = 1;
        Timestamp timestamp = 2;
        EndOfTrack end_of_track = 3;
    }
    
    message None {
        
    }
    
    message Timestamp {
        optional int64 timestamp = 1;
    }
    
    message EndOfTrack {
        
    }
}
