// Extracted from: Spotify 1.2.96.518 (Windows)

syntax = "proto3";

package spotify.playback_platform.context.v1;

import "google/protobuf/duration.proto";

option java_multiple_files = true;
option optimize_for = CODE_SIZE;
option java_package = "spotify.playback_platform.context.v1";

message LedgerQuery {
    bool enabled = 1;
    google.protobuf.Duration window = 2;
    
    EntryIdQuery entry_query = 3;
    message EntryIdQuery {
        bool include_ids = 1;
        
        oneof _filter_with_prefix {
            string filter_with_prefix = 2;
        }
        
        oneof _filter_with_suffix {
            string filter_with_suffix = 3;
        }
    }
    
    bool include_uids = 4;
    bool include_tags = 5;
}
