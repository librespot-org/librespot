// Extracted from: Spotify 1.2.96.518 (Windows)

syntax = "proto3";

package spotify.ads.esperanto.proto;

import "google/protobuf/empty.proto";

option objc_class_prefix = "ESP";
option java_multiple_files = true;
option optimize_for = CODE_SIZE;
option java_package = "com.spotify.ads.esperanto.proto";

service PodcastTesting {
    rpc GetNextAdSegment(GetNextAdSegmentRequest) returns (GetNextAdSegmentResponse);
    rpc OverrideMetadata(OverrideMetadataRequest) returns (google.protobuf.Empty);
}

message PodcastAdSegment {
    string slot_id = 1;
    uint64 start_ms = 2;
    uint64 stop_ms = 3;
}

message GetNextAdSegmentRequest {
    
}

message GetNextAdSegmentResponse {
    Status status = 1;
    enum Status {
        OK = 0;
        NOT_FOUND = 1;
    }
    
    PodcastAdSegment segment = 2;
}

message OverrideMetadataRequest {
    repeated PodcastAdSegment segments = 1;
}
